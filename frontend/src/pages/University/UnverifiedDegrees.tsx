import React from 'react'
import { Heading } from '../../components/general/Heading'
import Layout from '../../components/general/Layout'

const UnverifiedDegrees = () => {
    return (
        <Layout>
            <Heading text='Unverified Degrees' />
        </Layout>
    )
}

export default UnverifiedDegrees